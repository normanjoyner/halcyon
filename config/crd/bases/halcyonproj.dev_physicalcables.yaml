---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: physicalcables.halcyonproj.dev
spec:
  group: halcyonproj.dev
  names:
    kind: PhysicalCable
    listKind: PhysicalCableList
    plural: physicalcables
    shortNames:
    - cable
    singular: physicalcable
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .spec.sourceInterface
      name: Source Interface
      type: string
    - jsonPath: .spec.targetPort
      name: Target Port
      type: string
    - jsonPath: .metadata.labels.topology\.halcyonproj\.dev/datacenter
      name: Datacenter
      type: string
    - jsonPath: .metadata.labels.topology\.halcyonproj\.dev/availability-zone
      name: Availability Zone
      type: string
    - jsonPath: .metadata.labels.topology\.halcyonproj\.dev/region
      name: Region
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PhysicalCable is the Schema for the physicalcables API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: PhysicalCableSpec defines the desired state of PhysicalCable
            properties:
              sourceInterface:
                type: string
              targetPort:
                type: string
            required:
            - sourceInterface
            - targetPort
            type: object
          status:
            description: PhysicalCableStatus defines the observed state of PhysicalCable
            properties:
              state:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
