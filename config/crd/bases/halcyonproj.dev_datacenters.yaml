---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: datacenters.halcyonproj.dev
spec:
  group: halcyonproj.dev
  names:
    kind: DataCenter
    listKind: DataCenterList
    plural: datacenters
    shortNames:
    - dc
    singular: datacenter
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.provider
      name: Provider
      type: string
    - jsonPath: .spec.location.availabilityZoneName
      name: Availability Zone
      type: string
    - jsonPath: .metadata.labels.topology\.halcyonproj\.dev/region
      name: Region
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DataCenter is the Schema for the datacenters API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DataCenterSpec defines the desired state of DataCenter
            properties:
              location:
                properties:
                  address:
                    type: string
                  availabilityZoneName:
                    type: string
                required:
                - availabilityZoneName
                type: object
              provider:
                type: string
              type:
                enum:
                - private
                - colocation
                type: string
            required:
            - location
            - provider
            - type
            type: object
          status:
            description: DataCenterStatus defines the observed state of DataCenter
            properties:
              conditions:
                items:
                  description: DataCenterCondition defines conditions of the DataCenter
                  properties:
                    lastHeartbeatTime:
                      type: string
                    lastTransitionTime:
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  required:
                  - lastHeartbeatTime
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
            required:
            - conditions
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
